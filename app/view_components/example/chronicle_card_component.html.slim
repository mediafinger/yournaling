= render(Example::CardComponent.new(variant: :elevated, accent: "accent", interactive: href.present?, class: "ex-chronicle-card")) do |card|
  - if cover
    - card.with_media do
      .ex-chronicle-card__cover
        = image_tag(cover, alt: "", loading: "lazy")

  - card.with_header do
    .ex-stack.ex-stack--sm
      .ex-cluster.ex-cluster--between
        = render(Example::EyebrowComponent.new("Chronicle"))
        = render(Example::BadgeComponent.new(visibility[:label], variant: visibility[:variant], icon: visibility[:icon]))
      = render(Example::HeadlineComponent.new(level: 3)) { href ? link_to(title, href, class: "ex-link ex-link--cover") : title }
      span.ex-chronicle-card__dates
        = render(Example::IconComponent.new("calendar", size: :sm))
        = date_range

  .ex-stack.ex-stack--sm
    p.ex-chronicle-card__summary = summary

    - if entries.any?
      .ex-timeline
        - entries.first(4).each do |entry|
          .ex-timeline__item
            span.ex-timeline__date = entry[:date]
            div.ex-timeline__title = entry[:title]

  - card.with_footer do
    .ex-cluster.ex-cluster--between style="width: 100%"
      .ex-meta
        = render(Example::AvatarComponent.new(name: author, size: :sm))
        strong = author
        span.ex-meta__sep
        span = team
      .ex-cluster.ex-cluster--sm
        = render(Example::BadgeComponent.new("#{memory_count} memories", variant: :gold))
        - if href
          = render(Example::LinkComponent.new("Open", href: href, variant: :standalone))
