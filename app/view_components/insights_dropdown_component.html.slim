- trigger_class = active? ? "yui-nav-item yui-nav-item--strong" : "yui-nav-item"
= render(Yui::MenuComponent.new("Insights", trigger_class: trigger_class)) do
  - items.each do |label, path|
    = render Yui::MenuItemComponent.new(label, href: path, active: active_path?(path))
