#chronicles
  - if @chronicles.any?
    .yui-record-grid
      - @chronicles.each do |chronicle|
        = render partial: "teams/chronicles/chronicle", locals: { team:, chronicle: }
  - else
    = render EmptyStateComponent.new(icon: "📖", title: "No chronicles published yet", description: "This team has not published any chronicles yet.")
