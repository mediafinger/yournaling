= render partial: "form", locals: { thought: @thought, form_legend: "Edit thought" }
= link_to "Show this thought", admin_thought_path(@thought), role: "button"
= button_to "Destroy this thought", admin_thought_path(@thought), method: :delete, class: "secondary"
