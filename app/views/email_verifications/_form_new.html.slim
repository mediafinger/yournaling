.yui-narrow
  = render Yui::CardComponent.new do
    = render Yui::HeadlineComponent.new(form_legend, level: 3)
    = form_with url: email_verification_path, builder: YuiFormBuilder, html: { class: "yui-stack" } do |form|
      = form.email_field :email, required: true, autofocus: true, autocomplete: "email"
      = form.submit "Send Confirmation Link"
