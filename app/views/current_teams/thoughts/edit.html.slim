- if current_team == @thought.team
  = render partial: "form", locals: { thought: @thought, form_legend: "Edit thought" }
= link_to "Show this thought", current_team_thought_path(@thought), role: "button"
- if current_team == @thought.team
  = button_to "Destroy this thought", current_team_thought_path(@thought), method: :delete, class: "secondary", data: { turbo_confirm: "Are you sure you want to destroy this thought?" }
