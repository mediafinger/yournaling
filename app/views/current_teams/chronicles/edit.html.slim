.yui-narrow.yui-stack
  = render partial: "form", locals: { chronicle: @chronicle, form_legend: "Edit chronicle" }
  .yui-cluster
    = render Yui::ButtonComponent.new("Open", href: current_team_chronicle_path(@chronicle), variant: :secondary)
    = render PostDestroyModalComponent.new(post: @chronicle, name: "chronicle")
