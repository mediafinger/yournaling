= render partial: "form", locals: { memory: @memory, form_legend: "Edit memory" }
= link_to "Open", current_team_memory_path(@memory), role: "button"
= button_to "Destroy this memory", current_team_memory_path(@memory), method: :delete, class: "secondary", data: { turbo_confirm: "Are you sure you want to destroy this memory?" }
